#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
//extern 外部引入变量




//strlen()函数：查看字符串长度，返回一个int类型
//格式控制符%m，%0    输入时%m读取指定位数    输出时%m右对齐用空格补齐，%-m左对齐，%0m用0补齐位数
//如：scanf("%4d%2d%2d" &year, &month, &day)  printf("%d%02d%02d",year,month,day)
//Sleep(ms)函数
//system（）库函数，执行系统指令。system（“cls”）清空打印屏幕
//strcmp(str1,str2)库函数：比较字符串是否相等(头文件<string.h>)
//goto 关键字，一般用于跳出多层嵌套循环
//point:
//goto point;
//形参为数组：由于拷贝整个数组对内存浪费过大，所以形参只接收数组实参的指针，也就是形参数组名变成了一个指针变量
//数组指针传参时，在函数内部也可以看到外部的传入数组。在监视器输入a，10其中a为形式参数数组名，10为查看几个元素











